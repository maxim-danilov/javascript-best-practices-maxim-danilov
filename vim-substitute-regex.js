/**
 * Vim provides the :s (substitute) command for search and replace
 */

/**
 * Description: change each occurence in the current line
 * Command: s///g
 */

/**
 * Description: change each occurence in all lines
 * Command: %s///g
 */

/**
 * Description: change each occurence in selection
 * Command: '<,'>s///g
 */

/**
 * Description: wrap a key in quotes
 * Command: s/\(\w*:\)/"\1"/g
 * Before: callback: undefined,
 * After: "callback:" undefined,
*/
